{
  "dataset_query": {
    "database": 67,
    "type": "native",
    "native": {
      "query": "WITH g AS (SELECT CASE WHEN {{granularity}}='monthly' THEN CAST(DATE_FORMAT(created_at,'%Y-%m-01') AS DATE) ELSE DATE(created_at) END AS period, COUNT(*) n FROM reservations WHERE deleted_at IS NULL AND status=3 GROUP BY 1)\nSELECT period, CASE WHEN {{granularity}}='cumulative' THEN SUM(n) OVER (ORDER BY period) ELSE n END AS completed\nFROM g ORDER BY period\n",
      "template-tags": {
        "granularity": {
          "type": "text",
          "id": "gran-tag",
          "name": "granularity",
          "display-name": "Granularity",
          "default": "daily"
        }
      }
    }
  },
  "display": "line",
  "visualization_settings": {
    "graph.dimensions": [
      "period"
    ],
    "graph.metrics": [
      "completed"
    ]
  }
}
